const user = {
        username: "sanyamexam@gmail.com",
        passsword: "1234",
        notification: [],
    }
    //one cannot diectly do the equality by doing the simple ===
const num = user.notification;
if (user.notification === num) {
    console.log("true");
}

/// 
if (!user.notification === 0) {

}
//      it is not easy to do equality easily i  javascript
//